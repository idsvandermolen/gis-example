[tasks.compose-build]
description = "Build services with podman compose"
run = "podman compose build"

[tasks.compose-up]
description = "Start services with podman compose"
run = "podman compose up -d"

[tasks.compose-down]
description = "Stop services with podman compose"
run = "podman compose down"

[tools]
node = "latest"
